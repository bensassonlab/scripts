These scripts cannot work as standalone scripts. They ran on our local high performance computing cluster, and have many dependencies (other local scripts). They're here as a reference for detailed methods e.g. how did I make the figures in R?
